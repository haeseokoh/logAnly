# 대용량 문서 RAG와 구조화된 메모리 Q&A

이 문서는 `realtime-voice-chat-2` 대화에서 정리된 “대용량 문서를 위한 RAG와 구조화된 메모리” 내용을 질문과 답변 형식으로 다듬은 것이다. 원문의 핵심 개념과 예시는 유지하되, 반복되는 설명을 합치고 구현 판단이 드러나도록 재구성했다.

## 1. 기본 개념

### Q1. 대용량 문서를 LLM에 한 번에 넣으면 안 되나요?

대부분의 경우 권장하지 않는다. 문서가 모델의 컨텍스트 한도를 넘을 수 있고, 한도 안에 들어가더라도 중요한 근거가 긴 입력 속에서 희석될 수 있다. 입력 토큰과 처리 시간도 불필요하게 증가한다.

대신 원문을 검색 가능한 청크로 나누어 Knowledge Base에 저장하고, 현재 질문이나 작업에 필요한 청크만 검색해 LLM에 제공하는 방식이 적합하다. LLM은 전체 문서를 기억하는 역할이 아니라, 검색된 근거를 바탕으로 추론하고 답변하는 역할에 집중한다.

### Q2. RAG는 정확히 어떤 문제를 해결하나요?

RAG(Retrieval-Augmented Generation)는 생성 전에 관련 원문 근거를 검색해 모델 입력에 추가하는 구조다. 모델의 내부 기억이나 짧은 요약만으로 답하지 않고, 질문과 관련된 실제 문서 내용을 다시 찾아 답변을 뒷받침한다.

핵심 목적은 다음과 같다.

- 컨텍스트에 들어가지 않는 대형 문서를 다룬다.
- 정확한 표현, 예외 조건, 숫자와 결정 근거를 원문에서 복원한다.
- 답변에 문서명, 섹션, 줄 범위 같은 검증 가능한 출처를 남긴다.
- 작은 LLM도 필요한 근거만 제공받아 긴 문맥을 다룰 수 있게 한다.

### Q3. 구조화된 메모리와 RAG는 같은 것인가요?

아니다. 두 구성 요소는 서로 보완한다.

구조화된 메모리는 지금까지 확인된 상태를 짧고 안정적으로 압축한다. 예를 들면 엔터티, 결정, 제약, 진행 상태, 미해결 질문을 JSON 형태로 보관할 수 있다. 매 단계마다 이 메모리를 제공하면 모델이 장기 작업의 현재 상태를 빠르게 파악할 수 있다.

RAG는 압축 과정에서 빠진 세부 내용을 원문에서 복원한다. 과거 결정의 정확한 문장, 예외 조건, 정책의 근거, 수치 등이 필요할 때 관련 청크를 다시 검색한다.

정리하면 “압축 메모리는 항상 제공하고, 원문 근거는 필요할 때 RAG로 복원한다”가 핵심 원리다.

## 2. 청크와 Knowledge Base

### Q4. 청크는 무엇이며 왜 필요한가요?

청크는 원문을 검색과 LLM 전달에 적합한 크기로 나눈 텍스트 단위다. 문서 전체 대신 청크를 저장하면 질문과 의미적으로 가까운 일부 내용만 빠르게 찾을 수 있다.

청크를 만들 때는 단순 문자 수뿐 아니라 문서 구조를 고려해야 한다. 제목과 본문, 로그 레코드, 스택 트레이스, 표의 행처럼 의미가 이어지는 단위를 가능한 한 보존하는 편이 좋다.

### Q5. 청크에 텍스트만 저장하면 충분한가요?

충분하지 않다. 검색 결과를 검증하고 재사용하려면 메타데이터를 함께 저장해야 한다.

권장 메타데이터는 다음과 같다.

- 문서명과 내부 출처
- 섹션 또는 제목
- 원문 위치나 줄 범위
- 생성 시간 또는 로그 시간
- 작성자, 권한, 프로세스 등 도메인별 속성
- 앞뒤 청크를 찾기 위한 문서 내 순서

검색 결과는 정답 그 자체가 아니라 답변 후보 근거다. 출처가 보존되어야 사람이 원문을 다시 확인할 수 있다.

### Q6. 청크에 overlap은 왜 사용하나요?

고정 길이로 문서를 자르면 중요한 문장이 경계에서 끊길 수 있다. 이전 청크의 끝부분을 다음 청크에 일부 겹쳐 넣으면 문맥 단절을 줄일 수 있다.

다만 overlap을 크게 하면 저장량과 중복 검색이 늘어난다. 로그나 스택 트레이스처럼 명확한 구조가 있는 데이터는 구조적 경계를 우선하고, 일반 산문처럼 경계가 모호한 데이터에서 제한적으로 사용하는 것이 좋다.

## 3. 임베딩과 검색

### Q7. 임베딩 모델은 어떤 역할을 하나요?

임베딩 모델은 문서 청크와 질문을 같은 의미 공간의 벡터로 변환한다. 표현이 정확히 일치하지 않아도 의미가 가까운 텍스트는 가까운 벡터가 되므로 의미 기반 검색이 가능하다.

문서 적재 시 각 청크의 벡터를 한 번 생성해 저장하고, 질의 시에는 질문 벡터만 새로 만든다. 그 후 코사인 유사도나 내적을 사용해 가까운 청크를 찾는다.

### Q8. 키워드 검색과 벡터 검색 중 무엇이 더 좋은가요?

둘은 강점이 다르므로 함께 사용하는 것이 좋다.

키워드 검색은 오류 코드, 클래스명, PID, 정책명처럼 정확한 문자열을 찾는 데 강하다. 벡터 검색은 사용자가 원문과 다른 표현으로 질문해도 의미가 유사한 내용을 찾는 데 유리하다.

실용적인 구현은 FTS/BM25와 벡터 유사도 결과를 각각 구한 뒤 순위를 결합하는 하이브리드 검색이다. 점수 단위가 다른 경우 Reciprocal Rank Fusion처럼 순위 기반 결합을 사용하면 단순하고 안정적이다.

### Q9. 검색은 모든 청크 처리 단계마다 실행해야 하나요?

항상 실행할 필요는 없다. 직전 요약이나 인접 청크만으로 충분하면 검색을 생략할 수 있다.

다음과 같은 신호가 있을 때 조건부로 검색하는 것이 효율적이다.

- “이전”, “앞서”, “그 결정”, “해당 정책” 같은 과거 참조
- 새 엔터티나 새로운 주제 등장
- 현재 내용과 메모리 사이의 모순 가능성
- 정확한 수치, 정책, 근거 확인 필요
- 구조화된 메모리에 필요한 내용이 없음

이 방식은 검색 비용을 줄이면서 필요한 순간에는 원문 근거를 복원한다.

## 4. 멀티홉 RAG

### Q10. 한 번의 검색으로 답을 만들 수 없으면 어떻게 하나요?

첫 검색에서 얻은 단서를 이용해 다시 검색하는 멀티홉 RAG를 사용한다.

예를 들어 “환불 실패 재시도 정책”을 검색한 결과에 “PostgreSQL 트랜잭션”이라는 단서가 나온다면, 다음 단계에서 그 단서로 데이터베이스 선택 배경과 트랜잭션 제약을 검색할 수 있다. 첫 근거와 두 번째 근거를 함께 보면 정책과 기술 결정 사이의 연결을 설명할 수 있다.

### Q11. 멀티홉 검색에서 근거가 너무 많아지지 않나요?

각 검색 단계에서 후보를 제한하고 reranker로 재정렬해야 한다. 최종 프롬프트에는 토큰 예산 안에서 상위 근거만 넣는다.

근거가 여전히 많다면 다음 순서를 사용할 수 있다.

1. 검색 단계별 후보 수를 제한한다.
2. 중복 청크와 낮은 관련도 청크를 제거한다.
3. 인접 청크는 필요한 범위만 확장한다.
4. 근거 묶음별 요약을 만든다.
5. 요약과 핵심 원문을 최종 생성 단계에 전달한다.

요약만 제공하면 정확한 표현이 사라질 수 있으므로 중요한 결정과 숫자에는 원문 인용을 함께 유지하는 것이 좋다.

## 5. 구조화된 메모리 운영

### Q12. 구조화된 메모리에는 무엇을 저장해야 하나요?

일반적으로 다음 필드를 사용할 수 있다.

```json
{
  "entities": [],
  "decisions": [],
  "constraints": [],
  "open_questions": [],
  "recent_summary": ""
}
```

도메인에 따라 시간, 책임자, 상태, 관련 근거 ID 등을 추가할 수 있다. 메모리는 원문 복사본이 아니라 이후 작업에 필요한 상태의 압축 표현이어야 한다.

### Q13. 메모리를 자유 텍스트로 저장해도 되나요?

가능하지만 작은 LLM이나 장기 실행에서는 JSON 스키마처럼 구조를 제한하는 편이 안정적이다. 자유 텍스트는 필드가 누락되거나 의미가 섞이기 쉽고, 다음 단계에서 기계적으로 검증하거나 병합하기 어렵다.

권장 방식은 다음과 같다.

- 허용할 필드와 자료형을 정의한다.
- 모델 출력 JSON을 파싱하고 스키마를 검증한다.
- 검증에 실패하면 기존 메모리를 유지하거나 재생성을 요청한다.
- 중요한 결정, 날짜, 숫자는 출처 ID와 함께 저장한다.
- 새 정보가 기존 결정과 충돌하면 자동 덮어쓰기보다 검토 대상으로 표시한다.

### Q14. 메모리가 있으면 원문은 더 이상 필요 없나요?

아니다. 메모리는 압축본이므로 세부 조건이 사라질 수 있고, 잘못된 요약이 누적될 위험도 있다. 중요한 판단을 할 때는 반드시 원문 근거를 다시 검색하고 대조해야 한다.

메모리는 현재 상태를 빠르게 이해하기 위한 장치이고, 원문은 사실을 검증하기 위한 최종 근거다.

## 6. 모델과 실행 위치

### Q15. 임베딩 모델과 생성 LLM은 왜 분리하나요?

두 모델의 작업이 다르기 때문이다.

- 임베딩 모델: 청크와 질의를 벡터로 변환해 검색을 지원한다.
- 생성 LLM: 검색 근거와 메모리를 읽고 추론, 요약, 답변, 메모리 갱신을 수행한다.

임베딩은 비교적 작은 모델로도 충분하고 문서 적재 후 재사용할 수 있다. 생성은 더 복잡한 추론이 필요하므로 별도의 LLM을 사용할 수 있다. 두 역할을 분리하면 모델 교체와 자원 배분도 쉬워진다.

### Q16. 임베딩은 CPU, 생성 LLM은 GPU에서 실행해도 되나요?

가능하며 단순하고 실용적인 구성이다. 임베딩 모델과 검색 인덱스는 CPU에 상주시켜 문서 벡터와 질의 벡터를 처리하고, 생성 LLM은 GPU에서 답변을 만든다.

처리 순서는 보통 다음과 같다.

1. 문서를 청크로 분할한다.
2. CPU 임베딩 모델로 청크 벡터를 생성한다.
3. 벡터 인덱스에 청크와 메타데이터를 저장한다.
4. 질문을 임베딩하고 관련 청크를 검색한다.
5. 구조화된 메모리와 검색 근거를 프롬프트로 구성한다.
6. GPU의 생성 LLM이 답변과 메모리 갱신안을 만든다.

규모가 작으면 NumPy 내적만으로 검색할 수 있고, 데이터가 커지면 FAISS, Qdrant 같은 전용 검색 엔진을 검토할 수 있다.

### Q17. 작은 LLM으로도 장기 문서를 처리할 수 있나요?

가능하다. 모델이 전체 문서를 직접 기억하도록 요구하지 않고, 짧은 구조화된 메모리와 소수의 관련 원문 청크만 제공하면 된다.

작은 모델에서 안정성을 높이려면 다음이 중요하다.

- 프롬프트 역할과 출력 스키마를 명확히 한다.
- 검색 근거 수와 길이를 제한한다.
- 사실과 추론을 구분하도록 지시한다.
- 메모리 갱신 결과를 검증한 뒤 반영한다.
- 중요한 답변에는 원문 출처를 강제한다.

## 7. 구현 예시의 핵심

### Q18. 기본적인 구현은 어떤 함수들로 구성되나요?

최소 파이프라인은 다음 함수로 나눌 수 있다.

- `split_into_chunks`: 문서를 overlap이 있는 청크로 분할
- `build_index`: 모든 청크의 정규화 임베딩 생성
- `retrieve`: 질문 벡터와 인덱스의 유사도를 계산해 상위 청크 반환
- `needs_retrieval`: 현재 청크와 메모리를 보고 검색 필요 여부 판단
- `build_prompt`: 누적 메모리, 검색 근거, 현재 청크를 결합
- `generate`: 생성 LLM을 호출해 해석과 메모리 갱신안 생성

개념적인 처리 루프는 다음과 같다.

```python
chunks = split_into_chunks(document)
index = build_index(chunks)  # 문서 적재 시 한 번
memory = {
    "entities": [],
    "decisions": [],
    "open_questions": [],
    "recent_summary": "",
}

for chunk in chunks:
    if needs_retrieval(chunk, memory):
        evidence = retrieve(chunk, chunks, index)
    else:
        evidence = []

    result = generate(build_prompt(chunk, memory, evidence))
    memory = validate_and_merge(memory, result)
```

실제 서비스에서는 모델 출력 JSON 검증, 검색 결과 출처, 오류 재시도, 중복 제거, 토큰 예산 관리가 추가되어야 한다.

### Q19. NumPy 검색을 언제 전용 벡터 DB로 바꿔야 하나요?

문서와 청크 수가 작을 때는 정규화 벡터의 행렬 내적으로도 충분하다. 구현이 간단하고 외부 서비스가 필요 없다는 장점이 있다.

다음 요구가 생기면 전용 벡터 인덱스를 고려한다.

- 청크 수가 커져 전체 벡터 스캔이 느려짐
- 여러 사용자가 동시에 검색함
- 메타데이터 필터가 복잡해짐
- 증분 추가와 삭제가 빈번함
- 고가용성, 분산 처리, 백업이 필요함

전환 시에도 청크 ID와 출처 메타데이터를 검색 결과와 함께 반환하는 원칙은 유지해야 한다.

## 8. 정확성과 운영 안전성

### Q20. 검색 결과를 곧바로 정답으로 사용해도 되나요?

안 된다. 검색 결과는 관련성이 높은 후보일 뿐이며, 질문에 대한 충분한 근거인지 별도로 판단해야 한다. 유사하지만 다른 정책이나 다른 시점의 로그가 검색될 수도 있다.

생성 단계에서는 다음을 확인해야 한다.

- 근거가 질문의 대상과 같은 엔터티인지
- 문서 버전과 시간이 맞는지
- 예외 조건이 누락되지 않았는지
- 서로 충돌하는 근거가 있는지
- 답변의 각 핵심 주장에 실제 근거가 있는지

### Q21. 답변을 검증 가능하게 만들려면 어떻게 해야 하나요?

모든 청크에 출처 메타데이터를 저장하고 최종 답변에 사용된 근거 위치를 표시한다. 예를 들어 문서명, 내부 경로, 섹션, 줄 범위 또는 청크 ID를 인용할 수 있다.

LLM이 인용을 누락할 가능성에 대비해 애플리케이션이 실제 사용된 근거 목록을 답변 뒤에 별도로 출력하는 방식이 안전하다.

### Q22. 운영 환경에서 가장 중요한 주의점은 무엇인가요?

다음 원칙이 중요하다.

- 검색 결과와 생성 답변을 구분한다.
- 메모리 갱신은 스키마 검증 후 반영한다.
- 중요한 숫자와 결정은 원문을 다시 확인한다.
- 문서 권한과 데이터 접근 정책을 메타데이터와 검색 단계에 반영한다.
- 청크, 임베딩 모델, 생성 모델의 버전을 기록한다.
- 인덱스 재생성 중 기존 서비스 DB를 덮어쓰지 않는다.
- 장애 시 불완전한 인덱스를 게시하지 않는다.

## 9. 현재 logAnly 구현과의 연결

### Q23. 이 원칙들이 현재 Android bugreport 분석기에 어떻게 반영됐나요?

현재 `logAnly` 구현은 위 원칙을 Android bugreport에 맞게 적용했다.

- ZIP의 `main_entry.txt`가 가리키는 주 보고서를 줄 단위로 읽는다.
- 섹션, logcat 레코드, Java/native 스택 단위로 청크화한다.
- 섹션, ZIP 내부 출처, 줄 범위, 시간, 프로세스, PID, 심각도를 SQLite에 저장한다.
- SQLite FTS5와 `embeddinggemma` 벡터 검색을 결합한다.
- 관련 청크의 앞뒤 문맥을 확장한다.
- 출처 라벨을 포함한 근거 프롬프트를 `gemma4:e4b`에 제공한다.
- 답변 뒤에 실제 ZIP 파일과 원문 줄 범위를 출력한다.
- 원본 ZIP과 기존 인덱스를 직접 변경하지 않고 새 인덱스를 원자적으로 생성한다.

즉, 대화에서 논의한 “압축된 상태 + 필요할 때 원문 복원 + 검증 가능한 출처” 구조가 실제 로그 분석 도구로 구현된 형태다.

## 10. 한 문장 요약

### Q24. 전체 내용을 한 문장으로 요약하면 무엇인가요?

대용량 문서를 작은 LLM으로 안정적으로 다루려면, 현재 상태는 구조화된 메모리로 짧게 유지하고, 정확한 세부 내용은 청크·임베딩·검색 인덱스로 구성된 RAG에서 필요할 때만 복원하며, 모든 답변에 검증 가능한 원문 출처를 남겨야 한다.

## 원문 자료

- `C:\Users\hs\Documents\Codex\2026-08-24\realtime-voice-chat-2\outputs\RAG_knowledge_base_guide.docx`
- 원문 제목: “대용량 문서를 위한 RAG와 구조화된 메모리”
- 원문 부제: “청크, 임베딩, Knowledge Base, 조건부 검색, 멀티홉 RAG 구현 가이드”
